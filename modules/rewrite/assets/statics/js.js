var rewriteJs ={};

(function () {
    'use strict';

    jQuery(function () {
        rewriteJs.init();
    });

    rewriteJs = {
        config:{},
        init:function () {
        },
    }

})();